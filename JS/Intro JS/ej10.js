const FirstYearCompleted = true;
const YearsCompleted = 1;

console.log(YearsCompleted == FirstYearCompleted)
console.log(YearsCompleted === FirstYearCompleted)

//El primero compara si el valor de la variable es igual y el segundo solo da True si tanto la variable es del mismo tipo como también tiene el mismo valor. 
//En el primer caso, es True porque true=1 y el segundo es False porque no son el mismo tipo de variable (numérico y booleano).